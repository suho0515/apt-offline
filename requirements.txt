# In case users use it directly from source, please ensure following modules
# are installed
argparse
python-magic
soappy
# in ubuntu 20.04, python 3.3 include lzma library already.
# you don't need to install it again.
# lzma
pysimplesoap


# There's also a bunch of packages that need to installed
# On Debian systems, please ensure following packages, or their equivalent
# are installed

pyqt5-dev-tools
man2html-base
python3-debianbts
